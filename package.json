{
  "name": "@onigoetz/messageformat-parent",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "rm -rf packages/*/dist && lerna run build",
    "test": "lerna run test",
    "deploy": "node deploy.js"
  },
  "dependencies": {
    "lerna": "3.22.1"
  }
}