{
  "name": "@onigoetz/messageformat-parent",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "rm -rf packages/*/dist && oao run-script build --tree",
    "test": "oao run-script test",
    "deploy": "node deploy.js"
  },
  "dependencies": {
    "oao": "^1.10.0"
  }
}
