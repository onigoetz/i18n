name: Documentation

on:
  push:
    branches: [ master ]

jobs:
  documentation:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    - name: Generate documentation
      uses: docker://daux/daux.io:0.17.1
    - run: ls -la .
    - run: tree
    #- name: Deploy 🚀
    #  uses: JamesIves/github-pages-deploy-action@4.1.8
    #  with:
    #    folder: "static"
    #    branch: gh-pages
